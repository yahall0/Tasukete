<nav class="navbar navbar-expand-lg navbar-light bg-light navbar-custom py-1 sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Tasukete | 助けて | HelpOut</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item ">
          <a class="nav-link active" href="/new">Make a request</a>
        </li>
        <li class="nav-item ">
          <a class="nav-link active" href="/faq">FAQ</a>
        </li>
      </ul>
        <ul class="navbar-nav ms-auto">
          <% if(!currentUser) { %>
            <li class="nav-item">
              <a href="/google/login" class="nav-link active">Sign in with Google</a>
            </li>
          <%} else { %>
            <li>
              <img src="<%= currentUser.picture %>" class="profile-img" referrerpolicy="no-referrer" />
            </li>
            <li class="nav-item">
              <a href="/" class="nav-link active"> <%= currentUser.username %></a>
            </li>
            <li class="nav-item">
              <a href="/google/logout" class="nav-link active">Logout</a>
            </li>
          <% } %>
        </ul>
        
      
    </div>
  </div>
</nav>

<% if(success && success.length) { %>
  <div class="alert alert-success alert-dismissible fade show my-alert" role="alert">
      <%= success %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% } %>
<% if(error && error.length) { %>
  <div class="alert alert-danger alert-dismissible fade show my-alert" role="alert">
    <%= error %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>  
